<%= react_component(
  'App',
  {
    currentUser: ActiveModelSerializers::SerializableResource.new(current_user),
    game: ActiveModelSerializers::SerializableResource.new(Game.instance),
    teams: ActiveModelSerializers::SerializableResource.new(Game.instance.teams, include: %w[players])
  }
) %>
